# Módulo 3 - Bundling

## Vite (`modulo3-bundling/`)

- Obligatorio: TypeScript con chequeo de tipos, tamaño de bundle visible,
  scripts start/build/preview, variables de entorno dev/prod, h1 con DOM API + CSS Modules.
- Opcional: compresión GZIP y BROTLI en el build.
