# Módulo 3 - Bundling

## Webpack (`modulo3-bundling-opt/`)

- Parte 1: bundling básico con webpack, logo, hola mundo con SASS.
- Parte 2: migración a React + TypeScript, build de producción,
  variables de entorno dev/prod, medición de tamaño de librerías con webpack-bundle-analyzer.
